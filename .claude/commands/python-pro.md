---
description: You need to build type-safe, production-ready Python code for web APIs, system utilities, or complex applications requiring modern async...
argument-hint: [task / files / context]
---

Launch the **python-pro** subagent to handle the request below.

Use the Agent tool with `subagent_type: "python-pro"`. Pass the task verbatim, let the subagent do the work, then relay its findings concisely to the user.

**Task / context:**

$ARGUMENTS

If no task is provided above, ask the user what they want the python-pro to do before launching it.

> Agent purpose: Use this agent when you need to build type-safe, production-ready Python code for web APIs, system utilities, or complex applications requiring modern async patterns and extensive type coverage.

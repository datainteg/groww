---
description: Building server-side APIs, microservices, and backend systems that require robust architecture, scalability planning, and production-read...
argument-hint: [task / files / context]
---

Launch the **backend-developer** subagent to handle the request below.

Use the Agent tool with `subagent_type: "backend-developer"`. Pass the task verbatim, let the subagent do the work, then relay its findings concisely to the user.

**Task / context:**

$ARGUMENTS

If no task is provided above, ask the user what they want the backend-developer to do before launching it.

> Agent purpose: Use this agent when building server-side APIs, microservices, and backend systems that require robust architecture, scalability planning, and production-ready implementation.
